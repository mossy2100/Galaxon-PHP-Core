<?xml version="1.0"?>
<ruleset name="GalaCS">
    <description>Galaxon Coding Standard</description>

    <!-- Inherit from PSR-12 -->
    <rule ref="PSR12"/>

    <!-- Custom sniffs -->
    <rule ref="GalaCS.NamingConventions.ValidVariableName"/>
    <rule ref="GalaCS.Classes.ClassInstantiationNoBrackets"/>

    <!-- Allow multiple classes in test files for test fixtures -->
    <rule ref="PSR1.Classes.ClassDeclaration.MultipleClasses">
        <exclude-pattern>tests/*</exclude-pattern>
    </rule>

</ruleset>
