<?xml version="1.0"?>
<ruleset name="Galaxon">
    <description>Coding standard for Galaxon libraries</description>

    <!-- Register custom standard location -->
    <config name="installed_paths" value="GalaCS"/>

    <!-- Paths to check -->
    <file>src</file>
    <file>tests</file>
    <file>GalaCS</file>

    <!-- Galaxon Coding Standard (includes PSR12 + custom rules) -->
    <rule ref="GalaCS"/>

</ruleset>
